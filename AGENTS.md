# AGENTS.md

- This repository currently has no source files, manifests, lockfiles, CI config, or documented build/test/lint commands.
- Do not infer a framework, package manager, or app architecture from the repository name or screenshot files.
- The root currently only contains screenshot PNGs besides `.git`; treat them as reference/input assets unless the user says otherwise.
- When code or config is added, re-check root manifests and executable scripts before choosing commands.
